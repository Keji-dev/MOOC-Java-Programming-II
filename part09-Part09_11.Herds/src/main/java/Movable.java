// Interfaz que define los métodos que deben ser implementados por cualquier clase que quiera ser "movible".
public interface Movable {
    void move(int dx, int dy); // Método para mover el objeto.
    String toString(); // Método para representar el objeto como una cadena.
}
